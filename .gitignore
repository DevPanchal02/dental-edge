#~~~~~~~~~~~~~~~~~ Necessary Ignores ~~~~~~~~~~~~~~~~~#

# Node dependencies (inside client)
client/node_modules/

# Build output & cache (inside client)
client/dist/
client/.vite/

# === Data Files to Ignore (KEEP loader.js and the data directory itself) ===
# Ignore specific subject folders and all their contents
client/src/data/biology/
client/src/data/chemistry/
client/src/data/perceptual-ability/
client/src/data/reading-comprehension/
# Add any other subject folders you want to ignore here, e.g.:
# client/src/data/your-new-subject-folder/

# Ignore other specific data-related folders if they exist and should be ignored
client/src/new-data/
client/src/old-data/

# IMPORTANT:
# The specific folder ignores above (e.g., client/src/data/biology/)
# will correctly ignore those folders and their contents.
# The data directory itself (client/src/data/) is NOT ignored,
# so client/src/data/loader.js will remain tracked.

# Environment variables (inside client)
# Important: Uncomment if you use .env files! Keep secrets out of Git.
# client/.env
# client/.env.*
# !client/.env.example

# Log files (anywhere, including client)
*.log
client/npm-debug.log*
client/yarn-debug.log*
client/yarn-error.log*
client/pnpm-debug.log*

# IDE / Editor configuration
.idea/
.vscode/
*.sw?

# OS generated files
.DS_Store
Thumbs.db
._*